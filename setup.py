from setuptools import setup

setup(name='holidays',
      version='0.1',
      description='The disrupting package to check the school calendar :) ',
      url='https://github.com/WRPLA/holidays.git',
      author='brios',
      license='T4T-Rios',
      packages=['holidays'],
      zip_safe=False)
